<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:WarThunderParser.Properties"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="WarThunderParser.HelpWindow"
        Title="{x:Static res:Resources.btn_Help}" Height="640" Width="780" Background="Lavender">
    <Grid VerticalAlignment="Top" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <TextBlock Margin="5" TextAlignment="Left" TextWrapping="Wrap"  FontFamily="Prestige Elite Std" FontSize="13"
				Text="{x:Static res:Resources.dlg_Help_Content}" Height="Auto"       
                />
        </ScrollViewer>
    </Grid>
</Window>
