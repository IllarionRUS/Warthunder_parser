<Window x:Class="WarThunderParser.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:WarThunderParser.Properties"
        Title="{x:Static res:Resources.btn_Help}" Height="584.36" Width="1024" Background="Lavender">
    <Grid VerticalAlignment="Top" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <TextBlock Margin="5" TextAlignment="Left" TextWrapping="Wrap"  FontFamily="Prestige Elite Std" FontSize="13"
                Text="{x:Static res:Resources.dlg_Help_Content}"       
                >
            </TextBlock>
        </ScrollViewer>
    </Grid>
</Window>
